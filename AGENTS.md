# AI Agent Instructions

1. Read `README.md` first to understand the repository purpose and structure.
2. This repository manages dotfiles and configuration for a local environment.
3. When making changes, preserve the symlink-based workflow (files in this repo are linked into `$HOME` or other config paths).
